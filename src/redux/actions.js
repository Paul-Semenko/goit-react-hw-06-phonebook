import { createAction } from "@reduxjs/toolkit";

export const contactSubmit = createAction('contactSubmit');
export const changeFilter = createAction('changeFilter');
export const contactRemove = createAction('contactRemove');